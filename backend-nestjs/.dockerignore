# Node specific
node_modules
npm-debug.log
*.log
logs
dist
coverage
.env*
!.env.example

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor specific
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Git specific
.git
.gitignore
.gitattributes

# Docker specific
Dockerfile*
docker-compose*

# Other
*.md
*.yaml
*.yml

# Keep drizzle config if needed at runtime (unlikely)
!drizzle.config.ts

 # Keep migrations if applied at runtime (not recommended best practice)
!/src/database/migrations/
